services:
  # =============================================================================
  # PostgreSQL Database
  # =============================================================================
  postgres:
    image: postgres:16-alpine
    container_name: fotobook_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-fotobook}
      POSTGRES_USER: ${DB_USERNAME:-fotobook}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-fotobook} -d ${DB_DATABASE:-fotobook}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fotobook_network

  # =============================================================================
  # RabbitMQ (Message Queue)
  # =============================================================================
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: fotobook_rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-fotobook}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-secret}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_VHOST:-fotobook}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
      - ./docker/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
    ports:
      - "${RABBITMQ_PORT:-5672}:5672"
      - "${RABBITMQ_MANAGEMENT_PORT:-15672}:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fotobook_network

  # =============================================================================
  # Redis (Session & Cache)
  # =============================================================================
  redis:
    image: redis:7-alpine
    container_name: fotobook_redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fotobook_network

  # =============================================================================
  # PHP-FPM (Laravel Application)
  # =============================================================================
  app:
    build:
      context: ./web
      dockerfile: Dockerfile
      target: app
    container_name: fotobook_app
    restart: unless-stopped
    working_dir: /var/www/html
    environment:
      APP_ENV: ${APP_ENV:-production}
      APP_DEBUG: ${APP_DEBUG:-false}
      APP_URL: ${APP_URL:-http://localhost}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-fotobook}
      DB_USERNAME: ${DB_USERNAME:-fotobook}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CACHE_DRIVER: redis
      SESSION_DRIVER: redis
      QUEUE_CONNECTION: redis
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-fotobook}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-secret}
      RABBITMQ_VHOST: ${RABBITMQ_VHOST:-fotobook}
    volumes:
      - ./web:/var/www/html
      - ./web/storage/app/public:/var/www/html/storage/app/public
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - fotobook_network

  # =============================================================================
  # Nginx Web Server
  # =============================================================================
  nginx:
    image: nginx:alpine
    container_name: fotobook_nginx
    restart: unless-stopped
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
    volumes:
      - ./web:/var/www/html:ro
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
      - rabbitmq
    networks:
      - fotobook_network

  # =============================================================================
  # Queue Worker (Laravel Horizon or Queues)
  # =============================================================================
  queue:
    build:
      context: ./web
      dockerfile: Dockerfile
      target: app
    container_name: fotobook_queue
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
    environment:
      APP_ENV: ${APP_ENV:-production}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-fotobook}
      DB_USERNAME: ${DB_USERNAME:-fotobook}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      QUEUE_CONNECTION: redis
    volumes:
      - ./web:/var/www/html
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - fotobook_network

  # =============================================================================
  # RabbitMQ Image Fetch Worker
  # =============================================================================
  rabbitmq-worker:
    build:
      context: ./web
      dockerfile: Dockerfile
      target: app
    container_name: fotobook_rabbitmq_worker
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan rabbitmq:image-worker
    environment:
      APP_ENV: ${APP_ENV:-production}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-fotobook}
      DB_USERNAME: ${DB_USERNAME:-fotobook}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CACHE_DRIVER: redis
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-fotobook}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-secret}
      RABBITMQ_VHOST: ${RABBITMQ_VHOST:-fotobook}
    volumes:
      - ./web:/var/www/html
    depends_on:
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - fotobook_network

  # =============================================================================
  # RabbitMQ Image Upload Worker
  # =============================================================================
  rabbitmq-upload-worker:
    build:
      context: ./web
      dockerfile: Dockerfile
      target: app
    container_name: fotobook_rabbitmq_upload_worker
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan rabbitmq:upload-worker
    environment:
      APP_ENV: ${APP_ENV:-production}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-fotobook}
      DB_USERNAME: ${DB_USERNAME:-fotobook}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CACHE_DRIVER: redis
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-fotobook}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-secret}
      RABBITMQ_VHOST: ${RABBITMQ_VHOST:-fotobook}
    volumes:
      - ./web:/var/www/html
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - fotobook_network

  # =============================================================================
  # Node.js (Vite Dev Server - Development Only)
  # =============================================================================
  node:
    image: node:20-alpine
    container_name: fotobook_node
    working_dir: /var/www/html
    profiles:
      - dev
    command: sh -c "npm install && npm run dev"
    ports:
      - "${VITE_PORT:-5173}:5173"
    volumes:
      - ./web:/var/www/html
    networks:
      - fotobook_network

# =============================================================================
# Networks
# =============================================================================
networks:
  fotobook_network:
    driver: bridge

# =============================================================================
# Volumes
# =============================================================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  rabbitmq_data:
    driver: local
